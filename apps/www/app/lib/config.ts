/** The labs GitHub App's public slug — not a secret, it's part of the
 *  install URL that any signed-in user can navigate to directly. */
export const GITHUB_APP_SLUG = "heigvdlabs";

export const githubAppInstallUrl = `https://github.com/apps/${GITHUB_APP_SLUG}/installations/new`;
